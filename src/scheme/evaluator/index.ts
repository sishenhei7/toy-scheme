import type { INode } from '../node'
import type { Env } from '../env'
import type { Cont } from './continuation'

export class BaseEvaluator {
  public evaluate(node: INode, env: Env, cont: Cont) {

  }
}
